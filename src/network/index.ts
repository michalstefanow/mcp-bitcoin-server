// Placeholder for network provider interface and implementations
// This will define the common interface for SPV and API providers

/**
 * NetworkProvider Interface (to be implemented)
 * This interface will define methods like:
 * - getBalance(address: string): Promise<number>
 * - getUTXOs(address: string): Promise<UTXO[]>
 * - broadcastTransaction(txHex: string): Promise<string>
 * - getTransactionHistory(address: string): Promise<Transaction[]>
 */
export {};
